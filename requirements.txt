# Punish Tool — 메인 앱(프레임 분석기 + 코치) 의존성
# Python 3.11 기준. CUDA torch는 별도 인덱스에서 설치 권장.

# GUI
PySide6>=6.6
PySide6-WebEngine>=6.6

# 비전 / 영상
opencv-python>=4.8
numpy<2
Pillow>=10.0

# 딥러닝 (무브ID: DINOv2 / YOLO / TTS)
# CUDA 사용 시: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.4
torchvision>=0.19
ultralytics>=8.2          # YOLO pose

# OCR (HUD/입력표시 판독 보조)
pytesseract>=0.3.10

# 유틸
requests>=2.31

# ── TTS(Style-Bert-VITS2) 학습/합성은 별도 가상환경(tts/sbv2_env) 사용 ──
# torch==2.3.1+cu121, transformers==4.44.2, pyannote.audio==3.1.1,
# python-mecab-ko, g2pkk, jamo, num2words 등. 자세한 건 프로젝트_설명서.md 참고.

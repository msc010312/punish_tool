# 내일 할 일 (격투게임 프레임 분석기)

업데이트: 2026-06-23 밤. 끝낸 건 [x].

## 🔧 오늘 밤 고친 것 (데모 .exe 재빌드됨)
- [x] **속도 대폭 단축** — OCR 병렬화 + 프레임 8장 + 다운스케일 안 씀(정확도 유지). 3.18GB 기준 313초→~150초. (디코딩 96초가 남은 하한)
- [x] **JUST 오검출** — 4글자라 헛매칭 → 채택 기준 0.78로 엄격화. (재현되면 더 조이기)
- [x] **이벤트→코칭 리포트 점프 동기화** — 이벤트 클릭 시 영상+리포트 둘 다 이동.
- [x] **attacker 귀속 수정** — COUNTER/PUNISH 텍스트 뜬 쪽 = '한(때린)' 쪽으로. (3:27 1P 잡기펀ish 사례 기준 / ⚠ 아래 검증 필요)
- [x] **콤보 특수기 추천** — 빠른 기본기 + 특수기(214S·236K 등) 분리 표기.

## 🆕 추가 요청 (6/23 밤)
- [x] **(6) 디자인 개편 v2 (탭 분리)** — Claude 앱 스타일:
      탭1 **AI 코치**(중앙 인사+채팅, ＋영상 올리면 분석) / 탭2 **분석**(좌측 요약카드+주요이벤트, 중앙 영상+타임라인+리포트).
      AI 안 쓰는 사람은 분석 탭만. 주요 이벤트 좌측 배치. 둥근 카드·악센트·다크 테마로 감각적으로.
- [~] **(5) AI 채팅** — 채팅 UI 완성(로그+입력+전송). ⏳ 실제 LLM은 아직 데모응답 → **Claude API 연결 남음**(A의 AI휴먼 항목).
- [x] **(1) 카운터/저스트/퍼니쉬 각 횟수 표시** — 좌측 요약 카드에 P1/P2 분리 표.
- [x] **(2) 분석 진행바** — scan progress_cb → 상단 프로그래스바(0~100%).
- [x] **(4) exe 폴더 최상단 배치 / 트리 정리** — 소스 src/, 데이터·런처 루트, 문서 docs/.
      루트 `프레임분석기_실행.bat` 더블클릭 → 실행.
- [x] **(3) 모듈화(다른 게임 대비)** — `games/` 게임 프로필 추상화 완료.
      HUD좌표·체력색·텍스트색/위치·카운터레벨·프레임DB파일을 `games/ggst.py` 한 곳에.
      코어는 `games.ACTIVE`만 참조. 다른 게임 = `games/<game>.py` + REGISTRY 등록.
      ⏳ 남은 것: GUI 게임선택 메뉴에서 `games.set_active()` 연결(#6 디자인 때).
- [x] **(7) 트레이닝 vs 실전 구별** — `detect_mode()`: 맨 왼쪽 입력 프레임카운터 숫자 컬럼
      유무로 판별(트레이닝=숫자 있음, 매치=없음). 검증: 트레이닝→training, 매치→match 정확.
      트레이닝이면 분석 막고 "실전 영상을 넣어주세요" 메시지. 영역은 프로필(train_col).
- [x] **아이콘** — 119px PNG → 정사각 app.ico. exe 아이콘(--icon) + 창/작업표시줄 아이콘(setWindowIcon)
      + 루트 `프레임분석기.lnk` 바로가기(아이콘 적용). (.bat은 커스텀아이콘 불가라 .lnk로)

## ⭐ 우선순위 A — 정확도 핵심 (이게 안 되면 코칭이 틀림)
- [x] **자동 캐릭터 인식** — 이름판 OCR(흰 글자) → 33캐릭 퍼지매칭 → 영상 열면 P1/P2 자동 채움(다수결).
  - 검증: Sol vs Baiken 정확 인식. 미러전도 Sol vs Sol로 잡힘. 틀리면 사용자가 직접 수정 가능.
  - 영역은 게임 프로필(name_left/right)에 — 다른 게임 확장 시 좌표만 바꾸면 됨.
- [x] **attacker/victim 귀속 검증·수정** — 검증 결과 '텍스트 뜬 쪽'은 신뢰 불가(attacker/victim 왔다갔다).
  올바른 방법: **HP 깎이는 쪽(카운터 직후 콤보 대상)=victim, attacker=반대편**. analyze_match.nearest_hit_victim 콤보기반으로 수정.
  실제 영상 2건(24.24s/9.50s) ground-truth 검증 통과(텍스트측과 다른 케이스도 바로잡음). 콤보 없는 ~20건은 텍스트측 폴백.
- [ ] **AI 휴먼(코치) 기능** — 구조화 분석 → LLM이 사람 코치처럼 자연어 코칭.
  - 숫자/프레임은 엔진이, LLM은 말풀기만(환각 방지). **Claude API**(claude-opus-4-8/sonnet-4-6).
  - GUI에 'AI 코치' 탭, 이벤트 클릭 시 그 순간 멘트. 확장: TTS·페르소나 → 프리미엄 후보.

## 🎯 진행 중 — 무브 ID (애니메이션 CNN) ★내일 메인★
목표: 히트 순간 기술 식별(가벼운기까지) → 콤보·레벨·놓친펀ish 전부 정확해짐.
- [x] 데미지 기반 무브 ID v0 (무거운기 특정, `pe.identify_move`) — counter/damage 필드 스크랩 완료
- [x] **콤보 루트** — Dustloop 콤보 페이지 스크랩(`scrape_combos.py` → combos_ggst.json, 609개/32캐릭, 시동기색인).
      무브 ID→시동기→실제 초보콤보 추천 통합됨. (Testament만 0개)
- [x] **데이터셋 추출 도구**(`extract_move_dataset.py`) — 히트 순간 공격자 크롭+데미지 자동라벨,
      확실=`dataset/<캐릭>/<기술>/`, 애매=`_review/`. 영상1개→~20장.
- [x] **게임영역 자동 정규화** — `find_game_region`(검은띠 제거+16:9 보정)+`crop_game`,
      scan·캐릭인식·트레이닝판별에 통합. 검증: 16:9 no-op / 합성 레터박스(2100x1440)→정확히 16:9 복원.
      유튜버 오버레이(상단 랭크박스·하단중앙)는 HUD 안 가려서 OK. (울트라와이드/창모드 수동폴백은 추후)
- [x] **공격자 위치검출 = YOLO** — 범용 yolov8은 GGST에서 검출률 낮음(양쪽 ~43%).
      → **GGST 캐릭터 검출기 파인튜닝**(`build_detector_dataset.py` 부트스트랩 575장 → yolov8s 학습,
      mAP50 0.85, 양쪽검출 50%→62%). `runs/ggst_det/weights/best.pt`. `localize.py`가 이걸 우선 사용.
      환경: **RTX 4070, Python311에 torch 2.6+cu124(GPU)**. (`python -m pip` 사용 — `pip`은 Python310임)
- [~] **완전자동 트래킹**(`tracker.py` 속도예측+공백재앵커) — 라운드 경계 정체성 스왑 남아 보류.
      검출률 더 올리면 재시도 가능. 지금은 ↓ 반자동(B)로 데이터 확보.
- [x] **반자동 라벨러**(`label_tool.py`) ★채택★ — 트래킹 불확실성 우회.
      이벤트마다 깨끗한 두 크롭 중 **공격자 클릭(←/→) + 캐릭/무브 확정 + Enter저장**.
      검증: 한 영상 79이벤트 중 96% 양쪽크롭, 저장흐름 OK. 사람 보증 = 라벨 정확.
- [x] **시스템 메커닉 학습 대비**(`scrape_mechanics.py` → mechanics_ggst.json, 72섹션) —
      버스트/로망캔슬의 노란·금색 이펙트가 COUNTER로 오검출됨. **시각클래스 9개**
      (Red/Yellow/Purple/Blue RC, Gold/Blue Burst, Counter Blitz, Counter Hit, Overdrive)를
      라벨러 ⚙항목으로 라벨 → `dataset/__mechanics__/<클래스>/` → CNN이 구분 학습.
      게이지/규칙(Tension·RISC 등)은 이미지학습 불가라 참조용만.
- [x] **라벨러 개선** — 크롭=신뢰top2 박스(가짜 배제), **트레이드 양쪽저장**(Space=머무름),
      ⚙시스템 클래스 라벨(단서 표시), 크롭 고르면 캐릭 자동추정.
- [x] **데이터 정정: Wild Assault 삭제** — 2026.06 패치로 236D(Wild Assault)/236[D](Charged) 게임서 삭제됨.
      framedata에서 61개(전캐릭) 제거 + 스크래퍼에 제외필터. (유니버설 메커닉이라 per-캐릭 무브 아님)
- [ ] **데이터 축적** ← 12개 영상 + 추후 영상에 `label_tool.py` 돌려 라벨 (영상당 스캔 ~1-2분)
- [ ] **CNN 학습**(전이학습, GPU 준비됨) → 무브 + __mechanics__ 클래스 추론 통합

## 우선순위 B — 코칭 품질
- [ ] **OCR 미상 줄이기**(현재 ~110건) — 정확도 vs 속도 균형 더 잡기.
- [ ] **피격 검출 노이즈 튜닝**(DAMAGE_DROP/스무딩) — 가짜 피격·라운드 리셋.
- [ ] GUI 추가 피드백 반영.

## 우선순위 C — 큰 과제
- [ ] **거리 2차 필터 실작동** — 캐릭 x좌표 검출 + 기술 리치 데이터. (엔진 훅 준비됨)
- [ ] **무브 ID(최종 보스)** — 가드된 기술 식별 → '놓친 확정반격' 코칭.
- [ ] 초대형 영상 디코딩 속도 — ffmpeg hwaccel 시도 / 또는 낮은 비트레이트 녹화 권장.

---
### 파이프라인
scrape_dustloop→framedata / hud_calibrate→hud_config / hud_reader→timeline /
punish_engine(판정+특수기필터) / analyze_match→report / gui.py→FrameAnalyzer.exe
